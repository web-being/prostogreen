{
  "dependencies": {
    "sprae": "^5.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.11",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.3.1"
  },
  "scripts": {
    "start": "run-p serve tailwind",
    "serve": "jekyll serve",
    "tailwind": "npx tailwindcss -i ./src/main.css -o ./dist/main.css --watch"
  }
}
